{
  "opencollection": "1.0.0",
  "info": {
    "name": "True Fit - Consumer Token API",
    "version": "3.0"
  },
  "items": [
    {
      "info": {
        "name": "Token",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Get a new user session token, or reflect the current one if a valid token already present in the header. A token must be passed on all subsequent requests to the API.\n",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "/profile/public/v3/{tla}/token"
          },
          "docs": "Get a new user session token, or reflect the current one if a valid token already present in the header. A token must be passed on all subsequent requests to the API.\n"
        }
      ]
    }
  ],
  "bundled": true
}